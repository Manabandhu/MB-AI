# Notification domain

Channels: in-app, push, email, and SMS. Every request carries a stable event ID and template version. Evaluate authorization, user preference, consent, locale, timezone, quiet hours, urgency, deduplication, and rate limits before provider delivery.
