# Notifications

Owns the Notifications screen, notification center, preferences, permission education, device registration, deep-link handling, badges, and delivery-state presentation.

The in-app inbox route `/notifications` uses `api.ts` and `screens/NotificationsInboxScreen.tsx` against the read-only demo endpoint `GET /api/v1/notifications/inbox`.
