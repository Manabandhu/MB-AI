---
name: manabandhu-mobile-platform
description: Maintain ManaBandhu's Expo and React Native client across iOS, Android, phone, tablet, foldable, mobile browser, tablet browser, and desktop web. Use for routing, navigation, responsive layout, platform capabilities, accessibility, client state, offline behavior, or future watch/TV compatibility boundaries.
---

# Mobile platform

1. Read the Expo SDK 54 documentation, `docs/architecture/platform.md`, `frontend/src/platform/adaptive.ts`, and the affected module under `frontend/src/modules`.
2. Design by window size, input capability, safe area, orientation, font scale, and reduced-motion preference. Never branch on device model.
3. Keep phone, tablet, foldable, and web behavior in one Expo app unless an interaction model requires a separate shell.
4. Preserve keyboard, pointer, touch, deep-link, offline, accessibility, and responsive-web behavior.
5. Keep the Expo workspace directly under `frontend`. Keep routes thin and organize every business capability under `frontend/src/modules/<module>`.
6. Keep the adaptive operations surface at `/admin`; it may request allow-listed operations but must never receive infrastructure credentials or arbitrary execution capability. Its temporary public demo access is controlled server-side.
7. Address the workspace as `@manabandhu/frontend`; validate with `pnpm lint` and an Expo export for affected platforms.
8. Keep Expo export output (`dist/`, `dist-*`, and `web-build/`) ignored; CI uploads build artifacts rather than committing them.
9. Do not create watch or TV placeholder folders until those targets are activated.
10. Use Zustand 5 for shared client state; dependency resolutions must satisfy the workspace supply-chain release-age policy.
11. Treat `frontend/src/modules/screen-catalog.ts` as the canonical screen and route inventory. Keep Expo Router entries thin and use module-qualified IDs for repeated screen names.
12. The public launch flow begins at `/`, hides the native Expo splash after JS mounts, auto-advances the visible Stitch splash into `/welcome`, supports temporary demo sign-in to `/home`, and keeps the backend-status home surface at `/home`.
13. Keep reusable foundation constants, typed welcome API shapes, step helpers, and repeated welcome UI under `frontend/src/modules/foundation` rather than in Expo Router files.
14. First-batch Stitch routes through catalog screen 27 use thin Expo Router entries and module-owned screen components for auth, foundation, notifications, rooms, and rides.
15. The old local `stitch/` export folder has been removed; use a fresh MCP export as the source for the next Stitch implementation pass. Do not satisfy Stitch requirements by rendering screenshots as the app UI.
16. Shared catalog or Stitch-derived screens must expose connected navigation through primary nav, screen actions, and routed cards so route files are reachable through UI.
17. Shared UI primitives live under `frontend/src/modules/shared/ui`; use Gluestack-backed app wrappers such as `AppButton`, generated components under `shared/ui/gluestack`, and `AppIcon` before adding screen-local controls. Icons use `iconoir-react-native` with `react-native-svg`.
18. Keep Expo SDK 54 aligned through `expo install --fix`: React Native 0.81, React 19.1, React Native Web 0.21, and Node 20.19.4 or newer. Prefer a development build when a current physical-iOS Expo Go no longer supports SDK 54.
19. Update this skill when targets, breakpoints, routing, commands, dependencies, or invariants change.
20. Current frontend implementation: Expo SDK 54 with Expo Router, React Native 0.81, React 19.1, and React Native Web 0.21. Adaptive layout governed by `frontend/src/platform/adaptive.ts` using breakpoints compact (0), medium (600), expanded (1024), and wide (1440). All modules use `react-native-safe-area-context` and respect reduced-motion preferences.
  21. Recent client-side fixes: shared Supabase/client imports are grouped before UI imports, `AppButton` supports a `loading` disabled state, admin navigation uses typed `Href` casts where needed, `frontend/src/lib/apiClient.ts` centralizes shared API response parsing helpers (`parseJson`, `parseJsonOrThrow`) to replace duplicated module-local response handling, and duplicated `responseJson` functions were removed from module API files in favor of the shared helpers.
  22. Rides thin routes now cover `/rides/saved`, `/rides/mine`, `/rides/history`, `/rides/[rideId]/seat-requests`, and `/rides/[rideId]/participants`; missing routes must be created before module handoff.
  23. LoadingState component upgraded to replace static non-animated CSS ring with native `ActivityIndicator` (size="large", color=primary) and animated pulsing skeleton opacity loop (`0.4` to `1.0`), ensuring animated loading state across web, iOS, and Android.
  24. Real maps integration & Draw-to-Filter search: `react-native-maps` (1.27.2) installed and governed via `UniversalMapView` (`frontend/src/modules/shared/components/UniversalMapView.tsx` on native, `UniversalMapView.web.tsx` on web). On iOS native, uses Apple Maps (`MKMapView`) with zero setup/no token required. On Android, uses Google Maps. On Web, prioritizes Google Maps JavaScript API when `EXPO_PUBLIC_GOOGLE_MAPS_API_KEY` is configured in `frontend/.env`, uses Apple MapKit JS when `EXPO_PUBLIC_APPLE_MAPS_TOKEN` is present, and falls back to zero-configuration Leaflet/OpenStreetMap (CartoDB Voyager) if neither is set. Both Rooms and Rides map views wired to real interactive map pins and camera panning. Includes freehand sketch-to-filter search (Zillow/Apartments style) powered by `isPointInPolygon` ray-casting geometry in `geoPolygon.ts` and native/web polygon overlays.
  25. Saved rooms persistence & all-US city search: `useSavedRoomsStore` (`frontend/src/modules/rooms/savedRoomsStore.ts`) provides instantaneous local state and persistence across mobile/web with optional backend sync on `/api/v1/rooms/listings/{roomId}/favorite`. Room discovery metro selector includes full real-time search across all ~20,000 US cities via `searchAllUSCities()` in `locationService.ts` (`country-state-city`) with automatic map camera centering.
  26. Rooms mobile UI polish: Floating Action Button (FAB) for "+ Post a Room" anchored at bottom-right with elevation and high z-index without requiring a persistent mobile footer bar; removed redundant "Post Room" button from top header; eliminated cluttering "List View" toggle over map canvas; bottom sheets for city selection and filters feature swipe-down drag dismissal via PanResponder handle area and touch-outside overlay dismissal without needing explicit close buttons.
  27. Full-screen auth success celebration & error suppression: `AuthSuccessCelebration` is implemented inside a full-screen native/web `Modal` (`statusBarTranslucent`, `transparent`), ensuring a true 100% viewport takeover over both desktop split panes and mobile devices; `useAuthStore` rethrows auth failures, and `SignInScreen`, `SignUpScreen`, `EmailLoginScreen`, `PhoneLoginScreen`, and `OtpVerificationScreen` strictly suppress the celebration modal on failure, displaying inline error banners and only rendering celebration when session credentials succeed.
  28. Notification Settings Screen Overhaul: `/notifications/settings` modernized with Stitch aesthetic featuring delivery channel toggles (Push, In-App, Email, SMS/WhatsApp), granular module preferences (Rooms, Rides, Jobs, Events, Safety SOS), quiet hours bedtime schedule, master toggle with live status indicator, and toast confirmation for optimistic preference updates.

- Recent refactor: removed dead `shared/screens/Template*.tsx` files and `shared/fixtures/` directory; deleted unused `StitchPrototypeScreens.tsx` barrel. Added `useAdaptiveLayout` to foundation screens (`StitchAppShellScreen`, `CatalogContentScreen`, `StitchOnboardingScreen`, `StitchWelcomeFlowScreen`, `StitchSplashScreen`). Fixed `StitchAppShellScreen` to dispatch the correct shell API per `kind` (home/chat/explore/community/profile) instead of always calling `getHomeShell`. Consolidated duplicate fallback data between `homeShellFallbacks.ts` and `foundationScreenFallbacks.ts`.

- Recent auth guard changes: added `useRequireAuth('/sign-in')` to protected routes across referrals, expenses, events, marketplace, safety, admin, assistant, chat, community, notifications, rides, and rooms modules. Added `useRedirectIfAuthenticated('/home')` to auth routes (`/email-login`, `/phone-login`, `/otp-verification`, `/forgot-password`, `/reset-password`).
- Demo authentication uses a local session for `demo@manabandhu.local` / `DemoPass123`; regular password authentication remains Supabase-backed until the fixture user is provisioned.

- Recent CI fixes: added missing `CatalogScreenContent` imports to `ExpensesController`, `ImmigrationController`, `JobsController`, and `MarketplaceController` so the `@GetMapping("/screens/{screenId}")` endpoints compile; fixed `AssistantController` broken `PathVariable` import (`annotationPathVariable` -> `annotation/path/PathVariable`); fixed `RideRateScreen` mutation to pass the rating object directly instead of `(rideId, input)`; fixed `OfferReferralScreen` to declare `serviceType`/`availability` state and pass `serviceType` to `createReferralOffer`; fixed `RequestReferralScreen` to require `category` in `createReferralRequest` payload; fixed `CreateEventScreen` mutation payload to use backend-aligned `startAt`/`endAt`/`latitude`/`longitude`/`categoryId` fields; fixed `PostJobScreen` and `SellItemScreen` `TextArea` to use `watch`/`setValue` instead of `control.register` (the shared `TextArea` requires `value`/`onChangeText` props).

- Recent typecheck fixes: added `sent`/`time`/`body` to `AssistantMessage`, `body`/`meta`/`route` to `AssistantConversation`, and `body`/`meta`/`source` to `AssistantCitation`; switched `assistantFallbacks.ts` to `satisfies` casts; fixed `AssistantScreen` send payload from `{ text }` to `{ content }`; changed `chatFallbacks.ts` participant `role` from `"Member"`/`"Admin"` to `"MEMBER"`/`"OWNER"` and removed `senderName` from `Message` fallbacks; removed `params` props from `JobDetailsRoute`, `ListingDetailsRoute`, and `ReferralDetailsRoute` since the screens read `useLocalSearchParams` directly; fixed `StitchAppShellScreen` `AppIcon` name from `"exit"` to `"logout"`.

- Recent lint fixes: changed `CreateRideRatingInput` import in `RideRateScreen` to `import type` and reordered imports per Biome's `organizeImports` rule.
- Recent backend alignment: added `JobsContentService` (home, search, filters, saved, post), `ExpensesContentService` (home, groups, balances, settlements, add), `MarketplaceContentService` (home, search, categories, sell, saved), `ImmigrationContentService` (home, resources, guides, checklists, faq, questions, uscis, news, saved), `ReferralsContentService` (home, mine), `SafetyContentService` (center), and `UtilitiesContentService` (home). Added `/screens/{screenId}` endpoints to Jobs, Expenses, Marketplace, Immigration, Referral, and Utilities controllers. Added aliased routes to RoomsController and RidesController (`/{roomId}`, `/{rideId}`), plus `/mine`, `/history`, `/saved` on RidesController. Added `/search` on EventsController, `/{id}` and `/settings` on NotificationsController, and rewrote AssistantController with `/messages`, `/history`, `/citations` endpoints. Added `communityId` to CommunityPost entity with new controller endpoints.
- Recent AppIcon fix: added `LogOut` from `iconoir-react-native` and registered `logout` in `AppIconName` and `iconMap` so the shell logout action renders a real icon; added `Filter` and `FilterList` registered as `filter` and `filter-list` for quick filter and dedicated sort bottom sheet triggers.
- Recent Expo Router fix: flatten conditional styles on `Link asChild` children before passing them to Expo Router.
- Recent codebase fixes & hardening: added CSS module type declarations in `frontend/src/types/declarations.d.ts` and `frontend/expo-env.d.ts` to resolve TS2882 typecheck errors. Hardened web storage in `frontend/src/lib/supabase.ts` with in-memory storage fallback when `window.localStorage` is unavailable during SSR or static web export. Added unit tests for API response parsing with `node --test` under `frontend/test/`. Refactored admin screens to gracefully display fallback demo fixtures instead of full-screen error blocks when offline or unauthenticated. Made `StitchWelcomeFlowScreen` responsive with a `ScrollView`, bounded visual dimensions, and an interactive `Skip` button. Updated `SignUpScreen` to subscribe to `useAuthStore.error` and render inline error banners during registration and rate limiting.
- Stitch design system and entry flow alignment: integrated Stitch project `9663298292574415459` and design system `assets/18105029676315711182` ("ManaBandhu Modern Vibrant"). Extended design system color tokens in `packages/design-system` with `primaryContainer` and `warm`. Replaced entry assets with high-res brand emblem at `frontend/assets/images/brand-logo.png` and `splash-emblem.png`. Upgraded `StitchSplashScreen` (pulsing glowing logo, progress indicator, verified badge), `StitchWelcomeFlowScreen` (adaptive carousel, 4 community pillars, skip and get-started CTAs), `SignInScreen` (modern hero header, floating labeled inputs, lock/eye toggle, quick auth, trust badge), and `SignUpScreen` (live password strength meter, safety notice, quick social/phone auth) across web, iOS, and Android.
- Interactive Welcome carousel, desktop website designs & auth suite: enhanced `StitchWelcomeFlowScreen` with an interactive multi-slide carousel (4 pillars, next/prev controls, direct category chips, animated stepper dots, auto-timer with pause) and responsive desktop web layout (nav bar, 2-column hero, value checkpoints, stats ticker, full feature matrix, safety pledge, footer). Created `AuthPageLayout` delivering a responsive 2-column split layout on desktop web and fluid mobile view. Upgraded `PhoneLoginScreen` with an interactive country code selector dropdown modal and Gluestack Input, `EmailLoginScreen` (password visibility toggle, remember me, Apple/Google auth), `ForgotPasswordScreen` (recovery confirmation card, countdown), `ResetPasswordScreen` (password strength meter), `OtpVerificationScreen` (6-digit code input, resend countdown), and `ChooseLoginMethodScreen` (rich selector cards). Upgraded all auth and welcome screens to use exact vector icons from `AppIcon` (`iconoir-react-native`). Removed magic link routes and screens per design update.
- Authentic Brand SVGs & Inclusivity Refinement: Added official multi-color Google SVG and solid filled Apple SVG to `AppIcon` for high-fidelity social login actions; broadened community positioning to the global Desi community across welcome and auth screens; completely removed all mentions of "brokerage" from all modules in favor of direct community listings and transparent pricing; removed the green verified checkmark beside the "ManaBandhu" title in navigation and top bars.
- In-Place Seamless Auth & Post-Login Celebration: Upgraded `SignInScreen` and `SignUpScreen` with in-place segmented tabs (`Email` / `Phone OTP`) eliminating page hops; added inline OTP verification on the same card with countdown timer; added `AuthSuccessCelebration` animated takeover featuring bouncing glowing badge, pulse rings, trust pill, and animated progress bar before redirecting to `/home`. Enhanced `AuthPageLayout` with adaptive responsive widths for mobile, tablet, and desktop split view with keyboard tap handling.
- Embedded Country Dropdown, Universal Name Placeholders & Card Sizing Stability: Created `InlinePhoneInput` (`frontend/src/modules/auth/components/InlinePhoneInput.tsx`) to nest the country dial selector and inline dropdown directly inside the mobile phone input field, removing full-screen modal overlays. Universalized name inputs to "First and last name" across all forms. Stabilized `formContentBlock` dimensions with fixed minimum heights to prevent visual card jumping or resizing when toggling between Email and Phone tabs.

- Home/Explore/Chat/Community/Profile shell overhaul: Redesigned all 5 StitchAppShellScreen sub-shells to match Stitch designs (10 screens mobile+desktop). HomeShell: greeting+location chip, search bar, 2x4 quick-action grid, live-stats pills, horizontal room cards, community posts, job referral rows. ExploreShell: category chips, featured card, 2-col item grid. ChatShell: pinned alert, conversation rows with online dots+unread badges. CommunityShell: story bubbles, trending topics, post feed with reactions, FAB. ProfileShell: indigo gradient hero, stats row, trust badges, completion progress, quick links, account settings. Desktop layout (width>=768px) uses header nav links, no bottom tab bar.
- Root Layout Session Check: added useAuthStore.getState().checkSession() to frontend/src/app/_layout.tsx on mount so deep-linked routes initialize auth status properly.
- Style flattening on Link asChild: Wrapped Pressable style in StyleSheet.flatten inside HomeShell quick-action tiles to prevent Expo Router Slot invariant errors.
- Rooms Stitch UI: Overhauled RoomsScreen.tsx and RoomDetailScreen.tsx with rich UI cards, zero brokerage guarantee banners, flatmate compatibility tags, and gallery carousels with responsive web/mobile layouts.
- Real-Time Chat, Community Hubs & Expense Splitting Overhaul:
  - Real-Time Chat (`/chat`): implemented `useRealtimeChat` hook with Supabase Realtime channel subscription for postgres_changes on `chat_messages` and broadcast typing indicators; updated `ConversationScreen` with optimistic message rendering, sticky bottom bar, photo attachments (`expo-image-picker`), and structured inquiry cards; updated `ConversationInfoScreen` with participant roster, media gallery, and moderation actions; updated `ChatListScreen` with category tabs (`All`, `Direct Messages`, `Temporary Carpools`, `Rooms`, `Marketplace`).
  - Community Spaces (`/community`): updated `CommunityDetailsScreen` with 1-click Join toggle, active member count, and direct post navigation; updated `CreatePostScreen` with image attachment selector and tag filtering.
  - Expense Splitting (`/expenses`): updated `AddExpenseScreen` with split calculator (equal/exact amounts); enhanced `BalancesScreen` with pairwise "Who Owes Whom" net matrix and 1-tap "Settle Up" action; enhanced `SettlementsScreen` with historical audit logs.
- RoomForm Quick Chips & Resilient Creation: Added interactive pill selector chips for Room Type to streamline listing creation, autofill demo support, and resilient submission handling.
- Rooms Discovery Overhaul: Built interactive City Selector modal with live counts, live search input with clear button, modal filter sheet, quick sort chips, and interactive Map View with price pins, pulse rings, and floating detail cards. Upgraded RoomFavoritesScreen with Stitch aesthetics, back navigation, and fallback demo listings. Removed blocking auth guards from room discovery routes. Connected HomeShell Austin TX chip, 847 rooms placeholder, and live stat pills directly to /rooms.
- Responsive Bottom-up / Web Modals & Iconography: Adaptive Filters & Sorting modal renders as a bottom-up bottom sheet with handle on mobile viewports and as a centered card on desktop web. Added icons to amenities, filters, sorts, and categories. Seeded Supabase with live listings and wired queries through Spring REST endpoints. Cleaned banners and featured carousels per UX feedback.
- Rides Stitch Screen Overhaul: Generated mobile carpool discovery and ride details screens in Stitch MCP (Project ID `9663298292574415459`). Replaced placeholders in `RidesScreen.tsx` and `RideDetailScreen.tsx` with responsive carpool UI: route search bar with origin/destination swap, segmented Available Rides list vs Live Route Map view, horizontal quick filter chips, responsive bottom-up mobile / centered desktop filter & sorting modal, verified carpool cards with driver details and ratings, visual route timeline, interactive seat picker, community fuel split breakdown, and direct seat booking with `bookRideSeat()`. Unblocked discovery routes from forced authentication.
- Jobs Stitch Screen Overhaul: Generated mobile jobs discovery and job details screens in Stitch MCP (`6a09e96bf13b46e08ba1095d8dbb294d` and `fb5972ed58e147c6bfe599525567de6f`). Implemented rich UI with dual search input, live metrics, visa filter chips (H-1B, OPT, Green Card), internal referrer spotlights, compensation highlights, tech stack tags, and interactive referral request modal. Allowed public unauthenticated job discovery.
- Events Stitch Screen Overhaul: Generated mobile events discovery and event details screens in Stitch MCP (`f3ea9ea291424d759cf85d9786cc8bcc` and `203a0524f000487e9b1384e8d484e701`). Implemented vibrant cultural & professional meetups UI with live Supabase events feed, search bar, category chips, Austin Grand Diwali Mela hero card, step-by-step festival timeline, venue amenities, social proof attendee count, and digital RSVP pass ticket modal. Unblocked public unauthenticated event discovery.
- Marketplace Stitch Screen Overhaul: Generated mobile marketplace discovery and item details screens in Stitch MCP (`5d95554f3ad04b4c825641f92986fe4c` and `015eb6620ae544bc863d1ef4c7b044a3`). Implemented rich Telugu & Desi community marketplace UI in `MarketplaceHomeScreen.tsx` and `ListingDetailsScreen.tsx` with live Supabase listings and categories feed, 2-column responsive product card grid, price badges, negotiable and condition tags, verified seller badges, photo carousels, specification matrix, and interactive Make Offer negotiation modal. Unblocked marketplace item discovery from forced sign-in.
- Community Stitch Screen Overhaul: Generated mobile and desktop community discovery and post details screens in Stitch MCP (`478f613abe2b43588cc9bf4de319905f`, `3bca34efb3c64910bdc42f0dabd78dfb`, `1069f944595b40169bea28d225b52605`). Implemented rich UI in `CommunityHomeScreen.tsx` and `PostDetailsScreen.tsx` with Telugu community hub banner, topic chips, circle carousel, live discussion cards, verified author badges, moderator highlights, reaction metrics, comment tree, and quick reply bar. Allowed public unauthenticated community browsing.
- Referrals Stitch Screen Overhaul: Generated mobile referrals discovery and details screens in Stitch MCP (`84e1cfd6b5774d599713132163437272` and `3fd0de404d5f443da6b4c4cdb7e4ce76`). Implemented rich Desi referral network UI in `ReferralsHomeScreen.tsx` and `ReferralDetailsScreen.tsx` with live Supabase referral offers/requests feed, search bar, category chips, stats ticker, company logos, verified referrer profiles, eligibility requirements, what-to-prepare guidance, zero-brokerage guarantee, and interactive introduction request modal. Unblocked referral details from forced sign-in.
- Referrals fallback cleanup: removed `referralsFallbacks.ts` and inline demo fixtures across `ReferralsHomeScreen`, `ReferralDetailsScreen`, and `MyReferralsScreen` in favor of backend API queries, error states, and responsive empty states.
- Immigration fallback cleanup: removed `immigrationFallbacks.ts` and inline demo fixtures across all 10 immigration screens in favor of backend API endpoints, loading states, and responsive empty/error states.
- Rooms fallback cleanup: deleted `roomsFallbacks.ts`, removed `fallbackDetail` from `RoomDetailScreen` in favor of loading/error states, and removed `defaultSavedRooms` from `RoomFavoritesScreen` in favor of live favorites query.
- Rides fallback cleanup: deleted `ridesFallbacks.ts` in favor of live Supabase carpool queries, error handling, and responsive empty states.
- Jobs fallback cleanup: deleted `jobsFallbacks.ts` and removed inline fallbacks from `JobsSearchScreen` and `SavedJobsScreen` in favor of backend API endpoints and empty/error states.
- Events fallback cleanup: deleted `eventsFallbacks.ts` and removed inline demo data from `SavedEventsScreen`, `EventsSearchScreen`, `MyEventsScreen`, and `CreateEventScreen` in favor of live backend queries.
- Marketplace fallback cleanup: deleted `marketplaceFallbacks.ts` and removed inline demo data from `CategoriesScreen`, `MarketplaceSearchScreen`, and `SavedItemsScreen` in favor of live backend queries.
- Community fallback cleanup: deleted `communityFallbacks.ts` and removed inline demo data from `CommunityDiscoverScreen`, `CommunityJoinedScreen`, `CommunityDetailsScreen`, and `CreatePostScreen` in favor of live queries and error/empty states.
- Expenses fallback cleanup: deleted `expensesFallbacks.ts` and removed inline fallbacks from `ExpensesHomeScreen`, `ExpenseGroupsScreen`, `BalancesScreen`, `GroupDetailsScreen`, and `SettlementsScreen` in favor of live backend queries.
- Safety fallback cleanup: deleted `safetyFallbacks.ts` and removed inline fallbacks from `SafetyCenterScreen`, `BlockedUsersScreen`, `ReportsScreen`, and `TrustedContactsScreen` in favor of live backend queries.
- Utilities fallback cleanup: deleted `utilitiesFallbacks.ts` and removed inline fallbacks from `UtilitiesHomeScreen`, `NearbyScreen`, `EmergencyResourcesScreen`, and `PackageTrackingScreen` in favor of live backend queries.
- Foundation fallback cleanup: deleted `foundationScreenFallbacks.ts` and `homeShellFallbacks.ts`, removed fallback data from `CatalogContentScreen` and `StitchAppShellScreen` in favor of live backend queries.
- Admin fallback cleanup: deleted `adminFallbacks.ts` and removed fallback objects from all 9 admin screens in favor of live backend queries.
- Chat fallback cleanup: deleted `chatFallbacks.ts` and removed inline fallbacks from `ChatListScreen`, `NewChatScreen`, `ConversationScreen`, and `ConversationInfoScreen` in favor of live backend queries.
- AI Assistant fallback cleanup: deleted `assistantFallbacks.ts` and removed inline fallbacks from `AssistantScreen`, `AssistantHistoryScreen`, and `AssistantCitationsScreen` in favor of live backend queries.
- Foundation and Auth fixture cleanup: deleted `frontend/src/modules/foundation/fixtures.ts` and `frontend/src/modules/auth/fixtures.ts`. Removed hardcoded mock arrays (`sampleRooms`, `sampleJobs`, `samplePosts`, `liveStats`, `exploreItems`, `sampleConversations`, `communityFeedPosts`, `trendingTopics`, `storyAvatars`), demo CTAs, and static tickers in `StitchAppShellScreen` and replaced fake personas and metrics with qualitative community trust pillars in `StitchWelcomeFlowScreen`.
- Rooms hardcoded data cleanup: removed hardcoded host persona ("Karthik Raman"), static deposits/leases from `RoomDetailScreen` in favor of dynamic listing fields, removed demo autofill and mock submission from `RoomForm` requiring real auth, and removed static city count numbers in `RoomsScreen` calculating counts dynamically from raw listings.
- Rides hardcoded data cleanup: removed `DRIVER_METADATA` and `RIDE_DETAILS_DATA` dictionaries with fake personas and static co-riders in `RidesScreen` and `RideDetailScreen` in favor of dynamic fields derived from `RideOffer`, and made seat slot 2 dynamic based on `seatsAvailable`.
- Jobs hardcoded data cleanup: removed hardcoded referrer personas ("Sneha Murthy", "Vikram Patel", "Karthik Raman", etc.) and fake quotes in `JobsHomeScreen` and `JobDetailsScreen` in favor of generic community employee referral cards and direct submission.
- Community hardcoded data cleanup: removed hardcoded author persona ("Karthik Vemula") and initial "K" from `CommunityHomeScreen` and `PostDetailsScreen` in favor of dynamic author derivation from post entities, replaced hardcoded school assurance note with generic moderation guidelines, and removed static comment upvotes.
- Referrals hardcoded data cleanup: removed hardcoded referrer persona ("Phani Teja") and static response rates from `ReferralDetailsScreen` in favor of dynamic category and verified member badges, and replaced static ticker numbers in `ReferralsHomeScreen` with live counts derived from active offers and requests.
- Events hardcoded data cleanup: removed hardcoded attendee counts ("1,420 Desis Attending"), static capacity badges ("95% Capacity"), static carpool counts, and replaced static festival schedule in `EventDetailsScreen` with event-specific timeline derivation and generic schedule fallback.
- Marketplace hardcoded data cleanup: removed hardcoded seller persona ("Suresh Reddy") and static star rating from `ListingDetailsScreen` in favor of dynamic seller initial, member identifier, and verified resident badge.
- Notifications hardcoded data cleanup: removed `fallbackNotifications` from `NotificationsInboxScreen` in favor of direct live query and standard `EmptyState`, wired `NotificationDetailScreen` to `getNotificationDetail` with loading and error handling, and wired `NotificationSettingsScreen` to `getNotificationSettings`.
- Mobile LAN API resolution: added `getResolvedApiUrl` in `frontend/src/lib/api.ts` to dynamically resolve the backend API host across browser environments (`window.location.hostname`) and native Expo Go environments (`Constants.expoConfig.hostUri`), replacing `localhost` with the developer's LAN IP so mobile clients on the local network can connect seamlessly.
- Safety and Notifications Stitch overhaul & route fixes: modernized `SafetyCenterScreen.tsx` with emergency SOS banner and trust standards, modernized `NotificationsInboxScreen.tsx` with category filters and unread badges, and updated `quickActions` and `profileLinks` in `StitchAppShellScreen.tsx` to route to `/events`, `/marketplace`, `/safety`, `/events/mine`, `/immigration`, and `/notifications/settings`.
- Shared catalog and dynamic route param modernization: updated `FeatureScreen.tsx` and `CatalogScreen.tsx` with rich Stitch design system tokens, interactive card and action pressables, and soft ivory palette; fixed dynamic route param wrappers across `events/[eventId]`, `expenses/groups/[groupId]`, and `immigration/resources/[resourceId]` using `useLocalSearchParams`.
- Global search, saved items, settings, and multi-foldable responsiveness: overhauled `SearchScreen.tsx` with live multi-category search and popular suggestions, `SavedScreen.tsx` with tabbed views, and `SettingsScreen.tsx` with profile cards and notification toggles; added `isCompact` adaptations in `StitchAppShellScreen.tsx`, `EventsHomeScreen.tsx`, and `MarketplaceHomeScreen.tsx` to ensure seamless layout scaling across narrow foldable cover screens (e.g. Galaxy Z Fold 280-340px), standard smartphones, and unfolded tablet displays; added resilient offline/network fallback datasets to `SafetyCenterScreen.tsx`, `ExpensesHomeScreen.tsx`, `ImmigrationHomeScreen.tsx`, `UtilitiesHomeScreen.tsx`, and `NotificationsInboxScreen.tsx` to eliminate mobile network-blocking errors.
- Unified design system & backend implementation: Standardized and unified screens across Marketplace (`SellItemScreen`, `MarketplaceHomeScreen`, `ListingDetailsScreen`), Rides (`RideOfferScreen`), Chat (`ChatListScreen`), Notifications (`NotificationsInboxScreen`), Settings (`SettingsScreen`), and Admin (`AdminDashboardScreen`) to single design system (`#431ebe` Royal Indigo, `#00696b` Teal, `#ff7e33` Sunset Orange, `#faf8ff` surface). Wired interactive forms and mutations to backend REST endpoints.
- UI placeholder elimination & dynamic animations overhaul: replaced generic `FeatureScreen` placeholders with dedicated rich interactive screens for `ExpensesHomeScreen.tsx` (net balance card, quick splits, settlement modal), `ImmigrationHomeScreen.tsx` (live USCIS priority date ticker, category filters, legal consultations), and `UtilitiesHomeScreen.tsx` (diaspora essentials, tiffin & grocery carpools, provider contact sheet). Added tactile spring feedback animations to `AppButton.tsx` (`onPressIn`/`onPressOut`), dynamic pulsing halo to Emergency SOS banner in `SafetyCenterScreen.tsx`, and breathing glow pulse, pulse dot, and progress bar fill animations to `StitchSplashScreen.tsx`.
- Navigation reorder & form screen modernization: reordered main bottom navigation bar to `Home` -> `Community` -> `Explore` -> `Chat` -> `Profile`. Added universal client activity store (`useUserActivityStore`) and memoized selector hook (`useDynamicModules`) delivering dynamic, personalized quick action recommendations on `HomeShell` and master capabilities directory on `ExploreShell`. Modernized legacy form screens `CreatePostScreen.tsx`, `CreateEventScreen.tsx`, and `AddExpenseScreen.tsx` with rich category chips, topic tags, character counters, and brand design tokens.
- Day 02 Dynamic Onboarding Flow: implemented zero-hardcoded dynamic 8-step onboarding flow in `StitchOnboardingScreen.tsx` (`/onboarding/what-brings-you-here`, `/onboarding/location`, `/onboarding/languages`, `/onboarding/interests`, `/onboarding/profile-photo`, `/onboarding/notifications`, `/onboarding/trust-and-safety`, `/onboarding/complete`). Options for goals, diaspora metro locations, languages, community interests, notifications, and trust pledge items are dynamically fetched via `useOnboardingConfig()` from PostgreSQL/Supabase with dual API/Supabase reliability in `onboardingApi.ts`. User progress across steps is persisted to `user_onboarding_progress` with `useSaveOnboardingStep()` and finalized with `useCompleteOnboarding()`.
- Rooms & Housing Module Overhaul: Replaced hardcoded listings, amenities, and metro keyword arrays with dynamic catalog, REST search endpoints, and universal coordinate proximity matching (~45mi / 0.65°). Consolidated create and edit forms into reusable `RoomForm.tsx` with free offline geocoding (`country-state-city`, Photon OpenStreetMap API, Zippopotam.us). Wired 1-click chat handshake from inquiry screen to `/chat/[conversationId]`. Overhauled interactive map view and My Listings management with status tabs (Active, Draft, Rented) and 3-dot action menu.
- Rides Module Overhaul: Integrated Google Places autocomplete, Google Directions route polyline/distance/duration fetching, and toll preference calculator in `googleMapsService.ts`. Overhauled `RideOfferScreen` with fast 1-click re-post recent trip templates, tollway preference toggle (Avoid Tolls, Tolls Included, Split Tolls), and recurring daily commute schedule pills. Overhauled `RidesScreen` with toll filter chips, high-contrast departure badges, and recurrence badges. Upgraded `RideDetailScreen` with route timeline and 1-click ephemeral chat handshake. Upgraded `RideManageScreen` with trip lifecycle controls (start trip, mark completed with 2-hour chat purge notice, 1-click clone). Overhauled `RideMyListingsScreen` and `RideHistoryScreen` with Active Commutes tab and 1-click Re-activate Trip actions.
- Cross-Device Responsive UX Audit & Dual-Pane Reflow: Enhanced `frontend/src/platform/adaptive.ts` with `isDualPane` (width >= 1024px). Audited and updated Rooms and Rides screens: Small iOS devices (iPhone SE/8) wrapped in `KeyboardAvoidingView` with `keyboardShouldPersistTaps="handled"`, minimum 44x44pt touch targets with `hitSlop`; modern notched/Dynamic Island flagships respect `useSafeAreaInsets` on sticky top/bottom bars; budget Android devices handle hardware back navigation with `BackHandler` and FlatLists configure virtualization (`initialNumToRender={6}`, `maxToRenderPerBatch={10}`, `windowSize={5}`, `removeClippedSubviews={Platform.OS === 'android'}`); desktop/tablets render dual-pane side-by-side reflow for `/rooms` (feed + sticky map) and structured comparison table for `/rides/search`.
- Repo-Wide Code Review & Defect Remediation: Performed repository-wide defect audit, eliminated dead code and unused variables, added route parameter defensive guards across detail screens (`RideDetailScreen`, `JobDetailsScreen`, `ListingDetailsScreen`, `ConversationScreen`, `PostDetailsScreen`), enforced `KeyboardAvoidingView` and `keyboardShouldPersistTaps="handled"` on all form and chat inputs, added `hitSlop` on back buttons and touch targets, achieved 100% clean Biome linter check across all 321 files and 100% clean TypeScript `tsc --noEmit` check.
- Platform Compatibility & Web Animation: Set `useNativeDriver: Platform.OS !== 'web'` on all web-rendered React Native `Animated` sequences (including `SafetyCenterScreen`) to prevent web animated driver warnings during browser execution.
- Rooms & Rides Owner Control & Interactive Remediation: Added top navigation shortcuts for owner listings ("My Rooms" in `RoomsScreen`, "My Rides" in `RidesScreen`). Equipped cards with 1-tap **[🟢 Active / ⏸️ Deactivate]** toggle switches in `RoomListScreens`, `RideMyListingsScreen`, and `RideManageScreen`. Upgraded `RoomDetailScreen` with interactive swipeable image carousel, indicator dots, slide counter, and thumbnail strip. Remediated rides functionality: wired ride card chat button to `provisionRideChat` with direct routing to `/chat/${conversationId}`, made When and Commute Mode pills interactive, fixed native `Share.share` and backend bookmark mutations in `RideDetailScreen`, and enforced carpool seat availability bounds.
- Mobile Dynamic API Resolution & Stale Request Safeguards: Enhanced `getResolvedApiUrl` in `frontend/src/lib/api.ts` to dynamically resolve the backend host from `Constants.expoConfig.hostUri` (Metro host) on native mobile and `window.location.hostname` on web for any local or private LAN IP (`192.168.*`, `10.*`, `172.*`, `localhost`), ensuring mobile devices track developer IP changes automatically without getting trapped on stale subnets. Added 12-second abort timeout to `apiFetch` to prevent infinite loading state on network hiccups.


